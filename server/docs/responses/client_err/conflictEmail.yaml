description: 입력받은 이메일이 이미 데이터베이스에 존재함
content:
    application/json:
        schema:
            $ref: '../../schemas/Error.yaml'
        example:
            status: 4091
            message: 이메일이 이미 존재합니다
