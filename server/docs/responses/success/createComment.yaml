description: 댓글쓰기 성공
content:
    application/json:
        schema:
            $ref: '../../schemas/Success.yaml'
        example:
            status: 2000
            message: 댓글쓰기 성공
            result: { id: 2 }
