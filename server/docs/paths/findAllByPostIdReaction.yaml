get:
    summary: 글별로 추천 전체 조회
    tags:
        - Post Reaction
    parameters:
        - in: path
          name: postId
          required: true
          description: postId로 추천 전체 조회
          schema:
              type: number
              example: 1
    responses:
        2000:
            $ref: '../responses/success/findAllByPostIdReaction.yaml'
        5063:
            $ref: '../responses/server_err/dbSelectNot.yaml'
