get:
    summary: 글별로 댓글 전체 조회
    tags:
        - Comment
    parameters:
        - in: path
          name: postId
          required: true
          description: postId로 댓글 전체 조회
          schema:
              type: number
              example: 1
    responses:
        2000:
            $ref: '../responses/success/findAllByPostIdComment.yaml'
        4049:
            $ref: '../responses/client_err/noCommentContent.yaml'
        5063:
            $ref: '../responses/server_err/dbSelectNot.yaml'
